# 项目 AI 规范

## 验证

- 除非用户明确要求，不要执行 `pnpm build`。
- 除非用户明确要求，不要执行浏览器自动化、截图、视觉验收或页面交互测试。
- 代码修改后，不默认执行验证命令；仅在用户要求或修改风险明确需要时执行 `pnpm lint`。

## 前端约定

- 不要添加或修改任何 `aria-*` 属性。
- 不要额外添加 `role` 或辅助文本，除非用户明确要求。
- 保持项目为静态展示站点；未明确要求时，不引入 API 请求、环境变量、状态管理、数据请求缓存或后端能力。
- 页面需要兼容 PC 与 H5。
- 全屏页面使用 `100vh`、`100svh`、`100dvh` 的渐进高度策略，并考虑 `safe-area-inset`。

## CSS 规范

- CSS 自定义类名遵循 BEM：`.block`、`.block__element`、`.block--modifier`。
- 全局样式仅包含重置、设计变量、视口策略和跨页面共享样式。
- 所有 CSS 文件统一放在 `src/styles` 下。
- 每个页面单独导入自己的 CSS 文件；组件单独维护自己的 CSS 文件。
- 避免无边界的标签选择器、语义不明确的类名和跨组件样式覆盖。

## AI 规范维护

- 当用户提出会影响后续开发的长期约束、技术选型、代码风格、验证策略或项目边界时，自动更新本文件。
- 包含“始终”“默认”“以后”“每次”“不要再”“统一使用”“项目内必须”“禁止”等表述的要求，默认视为长期约束。
- 仅针对当前页面、当前组件或当前任务的一次性需求，不写入本文件。
- 无法判断需求是否长期生效时，先向用户确认。
- 更新本文件后，简要说明新增或调整的规则，不为此执行构建或验证。

## 修改原则

- 优先最小正确修改，避免无关重构和无用依赖。
- 新增菜单入口时，维护 `FloatingMenu` 中的菜单配置数组。
